# GROWTH Pilot #2 — Haddonfield Heights

Status: `EVIDENCE_COMPLETE` for the declared orientation task. No commit, push, or deploy.

## A. Content result

- Player Task: orient on Haddonfield Heights using confirmed landmarks and supported road relationships; recover when the landmark or route is not confirmed.
- Evidence Base rules exercised: `RULE-008` / `PAT-009` — lead with the exact place and use spatial proof only where available. GROWTH location/map status was `PENDING_VALIDATION` before this pilot.
- `RULE-008`: pilot result `PASS` for bounded location orientation. The page uses one first-party supported Lampkin Lane → Myers house relationship and does not invent a top-down map, spawn table, or fixed route.
- Evidence Pack readiness: `EVIDENCE_COMPLETE`; `OFFICIAL`, `VIDEO_OBSERVED`, `CROSS_SOURCE`, `INFERENCE`, and `UNKNOWN` remain explicit in `content-jobs/haddonfield-heights-growth-pilot-2/evidence-pack.json`.
- Selected functional visual: existing approved IllFonic flythrough frame at `0:12`, adjacent to residential-road orientation. It is supporting visual context, not named-landmark or route proof. No gallery was added.
- APIMart: Shared Article Writer, `gpt-5-mini`, `UPDATE`, successful output at `content-jobs/haddonfield-heights-growth-pilot-2/writer-output.md`. One initial Writer validation failure leaked an internal token; the same call was rerun with a stricter literal-token instruction and passed.
- Final page structure: quick answer → confirmed landmarks → orientation steps → wrong-turn recovery → static/dynamic evidence boundary → common mistakes → launch verification checklist → related next task → sources.
- Validation: `npm run verify:context` PASS; `npm run site:generate -- --spec site-spec.yaml` PASS; `npm run validate:generated` PASS; public-copy hygiene PASS (46 rendered files, 0 findings); local rendered page HTTP 200, canonical and required sections PASS.

## B. Shared Video Evidence reuse

- Public interfaces used: `prepareVideoIndex`, `buildEvidenceRequest`, `extractFramesFromIndex`, `captureFrames`, `evaluatePublicationQuality`.
- Cache: a new local cached low-res index was generated for `C2iqQykoqB8`; the previously approved `0:12` asset was reused as the page visual.
- Full low-res indexes downloaded: `1`.
- Targeted high-res windows: `3` requested (`0:05`, `0:12`, `0:35`); all captured as windows but publication QA rejected all because decoded native resolution was `640x360`.
- Manual `yt-dlp` / PO-token diagnosis: none.
- Custom Halloween-specific video code: none; only the shared public capability was called.
- Shared capability defects encountered: no execution-blocking defect. The available source stayed low-resolution for targeted windows, and the shared publication QA correctly prevented those frames from becoming proof.

## C. Efficiency compared with Pilot #1

- Codex execution rounds: `5` phase rounds — bootstrap/evidence query, shared video evidence, pack/dry-run, Writer UPDATE, integration/validation. This counts phase-level execution rounds, not individual shell calls.
- Manual intervention: one Writer prompt correction after validation caught leaked internal terminology; no video acquisition intervention.
- Repeated work avoided: no 44-winner research, no broad Halloween research, no full-video 4K search, no PO-token experimentation, no blind timestamp guessing, and no second video workflow.
- Remaining manual intervention causes: route claims still require a human evidence judgment when official prose does not establish map geometry; low-native-resolution material still needs either a better source or a future human capture task if named landmark proof becomes necessary.

## D. Files changed

Task-owned source and evidence:

- `site-spec.yaml`
- `site-input/pages/maps/haddonfield-heights.md`
- `content-jobs/haddonfield-heights-growth-pilot-2/` (intent brief, Evidence Pack, Writer output, video receipts, cached index, decoded frames, targeted-window receipts)
- `docs/growth-pilot-2-haddonfield-heights-report.md`

Generated by the required pipeline:

- `.site-generator-manifest.json`
- `src/config/site.generated.ts`
- `src/content/docs/map-guides/maps/haddonfield-heights.md`
- `public/pagefind/*` runtime-generated search artifacts

The pre-existing dirty pagefind files were preserved. No commit, push, deploy, or shared-dev-skills modification was made.
